# Environment
SECRET_KEY=6rh65mm3by-6k6v4xs@^q394c77u0@-2qguax*0qwb7gbo6

DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,192.168.29.1

# Database (defaults to SQLite if not set)
DATABASE_URL=sqlite:///db.sqlite3

# CORS — comma-separated origins allowed for Flutter/React
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
CORS_ALLOW_ALL=False

# JWT token lifetimes
JWT_ACCESS_LIFETIME_MINUTES=60
JWT_REFRESH_LIFETIME_DAYS=7

# OpenRouter — vision LLM (optional, for /detect/openrouter/)
OPENROUTER_API_KEY=your-openrouter-api-key
OPENROUTER_MODEL=anthropic/claude-3-haiku

# ArcFace — face recognition threshold (0.0–1.0)
ARCFACE_SIMILARITY_THRESHOLD=0.5
